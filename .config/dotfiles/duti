# duti - default-app associations, applied by macos.zsh.
# Columns: bundle-id  type  role. Leading '.' marks an extension; 'all' = viewer+editor.


# javascript / typescript
dev.zed.Zed .js   all
dev.zed.Zed .ts   all
dev.zed.Zed .tsx  all
dev.zed.Zed .jsx  all

# python
dev.zed.Zed .py   all

# ruby
dev.zed.Zed .rb   all

# config
dev.zed.Zed .json all
dev.zed.Zed .yaml all
dev.zed.Zed .yml  all
dev.zed.Zed .toml all

# markdown
dev.zed.Zed .md   all
